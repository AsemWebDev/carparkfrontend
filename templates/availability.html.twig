{% extends "base.html.twig" %}

{% block body %}
    {% form_theme form 'bootstrap_5_layout.html.twig' %}

    <h2>Check Availability</h2>
    <div class="row">
        <div class="col-4">{{ form(form) }}</div>
    </div>

    {% if available_spaces|length > 0 %}
        {% include 'include/spaces.html.twig' with {'available_spaces': available_spaces} %}
    {% endif %}
{% endblock %}
